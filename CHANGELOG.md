# Changelog

本技能由使用数据驱动迭代。每个版本记录：数据基线（任务数/时间范围）+ 规则变化。
格式参考 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/)。

## v1.3.0（2026-09-04）

**数据基线更新到 312 条（2026-07-21 → 2026-09-03，+40 条），新增 W9 工作流。**

### 新增

- **W9 透明底直出设计**（片头字幕/封面字幕/文字素材）：纯文字素材、无参考图时，
  prompt 末尾写“输出真透明底”直接出透明 PNG，**不用走绿幕抠像**（你 2026-09-03 起的做法，
  实测 3 次“输出真透明底”迭代）。
- 新句式入模板库：
  - 封面设计（图生图，必带截图/素材图，实测 13/13 带图）：`结合我的文案设计一个高级的视频封面（16：9）↵…`
  - 参考图+标题文字封面 / 结尾字幕图（封底）：`结合我的参考图与标题文字设计一个高级的视频封面图，[色调]↵…`
  - 挂角字幕条（角标式，W2 变体）：`参考@图1这个风格…设计一个视频用挂角字幕条（长条/一行排版/…）`
  - 风格参考设计（W2/W8 融合）：`参考@图1这个风格（配色色调图案元素，不要完全一致，有所改动）…`
- **生成类型全量核实并写入每条工作流**（312 条按 `inputImageIds` 统计）：
  W1/W3/W4/W5/W8 = 图生图（100% 带图）；W2/W6 混合；W7 文生图为主；
  **W9 = 唯一纯文生图的设计工作流**（透明直出只在这里成立）。
  修正 v1.3.0 初稿误标：“结合我的文案设计封面”实为图生图（带参考图），非纯文生图。
- 风格词库新增：`让人眼前一亮`、`符合主题`、`现代科技风`、`蓝金色调`、`主副标题大小不要差太多`、
  `不要出现XX元素`（负向约束防模型乱加题材元素）、`（示例文字）` 占位标注。
- `SOP-9 透明底直出设计`、W9 模板与分工规则（W3/W9 何时用哪个）。

### 变更

- 数据基线：272 → **312 条**（2026-07-21 → 2026-09-03）；`analyze.py --save-baseline` 已重存。
- 参数占比刷新：size auto 87%、quality auto 91%、n=1 88% / n=2 3% / n=3 1%、带参考图 76%。
- 词库计数刷新：高级感 107、设计感 94、字幕条类 87、绿底类 80、视频封面 69、挂角 11。
- SKILL.md 触发词扩展：`结合我的文案设计封面`、`设计片头字幕/开头字幕/透明底字幕`、`挂角`。

### 不变

- 9→仍 8 条既有工作流模板、三条铁律、n>1 分治策略、`--unkey`/`--transparent` 分工。
- 串行出图、1000s timeout、key 配置方式。

## v1.2.1（2026-09-04）

**修正 v1.2.0 的 `multipart EOF` 结论**（对照实验推翻了“感知指纹拦截”假设）。

### 修正

- **`multipart EOF` 真实机制**：对照实验（原图 2.6MB 在窗口期恢复后用 curl 与 Python
  两种客户端直传**均成功**；期间随机图/小图始终正常）证明：
  - **不是**图片内容被永久拉黑、**不是** size 问题、**不是**“感知指纹标记”
  - 是**同一图片短时间反复上传**（本次为 30 分钟内同一照片 10+ 次）触发的**临时上传限制**，
    实测窗口**约 1 小时、自行恢复**
  - 处理策略改为：脚本自动等 45s 重试一次；真被限制时**等 10~30 分钟再发**；
    窗口期急用可重编码参考图（改字节）绕开
- 修正 v1.2.0 中“批量 400 后惩罚窗口约 1~5 分钟”的表述（未证实，删除）

### 变更

- multipart 字段顺序与生成平台 `openaiCompatibleImageApi.ts` 对齐
  （model/prompt/size/output_format/moderation/quality/n，参考平台源码核实：
  平台为原文件字节直传、无重编码、仅 Authorization 头）
- 图生图串行 n=1 去掉“先等 20s”（窗口约 1 小时，短等待无意义）

## v1.2.0（2026-09-04）

**透明底规则修正（文生图/图生图分治）+ n>1 不稳定策略 + 上传故障兜底**
（实测环境：`https://ai-pixel.online`，模型 gpt-image-2）。

数据基线：同 v1.0.0（272 条任务）；本版为**协议层**更新，任务数据不变。

### 新增

- `--unkey` 选项：出绿底图后本地抠键色，输出带 alpha 的真透明 PNG（不改 prompt）；
  结果已带 alpha 时自动跳过抠色
- `--n 2` 图生图时直接串行 n=1；文生图先按 n>1 试 3 轮（10s 间隔，中转站账号随机
  分配，被拒时可能换到支持的账号）再退串行

### 修正

- **透明底只在文生图生效**（用户实测 + 脚本验证）：
  - 文生图（无参考图）：prompt“透明底”→ 直接返回真透明 PNG，且排版设计质量比绿底版更好
  - 图生图（带参考图）：prompt“透明底”不生效 → 改“背景设置为方便抠图的纯绿色背景”+ `--unkey`
- **n>1 行为不稳定**（“n greater than 1 is not supported”有时出现有时可以，
  与中转站分配的账号有关，用户实测）：不再写死“接口不支持”，改为上述分治策略
- `multipart EOF`（大文件上传被截断）：检测到后自动等 45s 再重试
  （批量 400 后的惩罚窗口约 1~5 分钟，短重试无效）；串行 n=1 每张重建 multipart（新 boundary）

### 新发现的故障模式（写入排错文档；v1.2.1 已修正结论）

- ~~感知指纹拦截~~（v1.2.0 假设，**v1.2.1 已推翻**）：现象为同一参考图反复上传后
  该图任何尺寸的上传被截断（`multipart EOF`）。v1.2.1 对照实验确认实际机制是
  **同一图片短时间反复上传触发的临时上传限制（约 1 小时、自行恢复）**

### 不变

- 8 类工作流模板、风格词库、迭代节奏、比例写进 prompt 铁律
- 绿幕抠图习惯（“方便抠图的纯绿色背景”）
- 串行出图、1000s timeout、key 配置方式

## v1.1.0（2026-09-04）

**接口端点迁移 + 协议行为实测修正**（实测环境：`https://ai-pixel.online`，模型 gpt-image-2）。

数据基线：同 v1.0.0（272 条任务）；本版为**协议层**更新，任务数据不变。

### 变更

- 默认端点 `https://speed.ai-pixel.online` → **`https://ai-pixel.online`**（用户告知端点变更；本机配置已同步）
- 实测证实：**平台参数 UI 不作用到出图，提示词是控制项**：
  - `size` 参数被中转站忽略（实测发 720x1280 返回 1672x941）→ **比例必须写进 prompt**（“（16：9）”→1672x941、“（9：16）”→941x1672，均实测）
  - prompt 含“透明底”→ **直接返回带 alpha 的真透明 PNG**（实测 73% 像素 alpha=0、无颜色残留、视觉验证棋盘格透出）→ 透明底不再需要 background 参数 / 平台选项 / 本地抠键色
- `--transparent` 降级为**兜底模式**（[背景指令]+本地抠键色保留，仅在返回图无 alpha 时使用）
- `gimg.py` 新增：出图后自动校验 alpha 通道（prompt 含“透明底”但返回无 alpha 时明确警告）
- 规则文档全面更新：`parameters.md`（§1/§2.1/§3/§6/§9）、`prompt-recipes.md`（比例与用途速查改为 prompt 写法）、`workflows.md`、`iteration-and-errors.md`（新增两条错误处理）
- 视觉验证：测试图经视觉模型复核（16:9 横版/9:16 竖版/透明底棋盘格透出，文字 OCR 无误）

### 不变

- 8 类工作流模板、风格词库、迭代节奏（短句+保护句）、n=1/n=2 规则
- 绿幕抠图习惯（“方便抠图的纯绿色背景”）
- 串行出图、1000s timeout、key 配置方式（env > ~/.gptimage > ~/.gptimage-ppt）

## v1.0.0（2026-09-01）

数据基线：**272 条任务**（2026-07-21 → 2026-09-01，268 成功 / 4 失败），
来源为 gpt-image-playground 三份导出备份合并去重。

### 新增

- 8 类工作流提示词模板（封面/字幕条批量/绿幕抠图/多图合成/就地改字/单张PPT封面/分镜图/风格复刻）
- 参数规则：size 默认 auto、quality 默认 auto、n=1 定稿 / n=2 探索、png、绿幕优先于透明
- `scripts/gimg.py`：直连 API 出图（纯标准库），包含——
  - `@图N`（含零宽标记）自动转 `[image N]`
  - 有参考图自动走 `images/edits`（multipart `image[]`），无参考图走 `images/generations`（JSON）
  - 单请求 n>1 被拒时自动退化为串行 n=1
  - `--transparent` 真透明底：追加平台同款 [背景指令] 块（#00FF00/#FF00FF）+ 本地键色抠除（纯标准库 PNG 编解码 + 与平台同算法的泛洪/去溢色）
  - key 读取：环境变量 `GPTIMAGE_API_KEY` > `~/.gptimage/config.json` > `~/.gptimage-ppt/config.json`
- 接口协议对照 gpt-image-playground 源码（github.com/CookSleep/gpt_image_playground）核实
  （端点 / mention 转换 / 透明底实现 / n>1 与 background 限制），并实测三条链路通过
- 跨平台安装脚本（Windows PowerShell / macOS·Linux sh）
- 迭代规则：短句修改 + 保护句“其他不变”、绿底每轮重申、先出图确认再批量
